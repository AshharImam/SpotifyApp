const {StyleSheet} = require('react-native');

const styles = StyleSheet.create({
  image: {
    height: 400,
    width: '100%',
  },
  title: {
    fontWeight: 'bold',
    fontSize: 24,
  },
  lowerContainer: {
    paddingHorizontal: 8,
  },
});

export default styles;
